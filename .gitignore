# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.env

# Node / Next
node_modules/
.next/
.next-dev/
out/
dist/
*.tsbuildinfo

# IDE
.vscode/
.idea/
# Ignora .claude/ em geral (caches, worktrees, configs locais, logs),
# mas versiona os subagentes de projeto em .claude/agents/.
.claude/*
!.claude/agents/
!.claude/agents/*.md

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Uploads (dev local)
uploads/

# Secrets — NUNCA commitar chaves JWT
keys/*.pem
keys/*.key

# Reports / outputs locais
/jobs/

# Celery beat schedule (runtime, pickled — nunca versionar)
backend/celerybeat-schedule

# Relatórios e2e gerados (Playwright)
tests-e2e/report/
tests-e2e/test-results/
tests-e2e/package-lock.json
frontend/test-results/

# Docker compose overrides locais (porta, env etc)
docker-compose.override.yml
